services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DATA_DIR=/app/data
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
